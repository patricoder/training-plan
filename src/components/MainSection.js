import { Section, Container } from "./MainSection.styles";

const MainSection = () => {
    return (
      <Section>
        <Container>abc</Container>
      </Section>
    );
};

export default MainSection;